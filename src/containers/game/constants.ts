import { Game } from './types';

const DEFAULT_GAME_OPTIONS: Required<Game.PlayerOptions> = {
	level: '1-1',
};

const StyleClassNames = {
	PixelArt: 'pixel-art',
};

export { DEFAULT_GAME_OPTIONS, StyleClassNames };
